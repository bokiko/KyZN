# KyZN's own config — this is how KyZN improves itself.
# For your project, run `kyzn init` to generate your own .kyzn/config.yaml.
#
# Generated by: kyzn init
# Date: 2026-03-18T10:37:33Z

project:
  name: kyzn
  type: generic  # KyZN is bash — no bash measurer yet, so generic is correct
preferences:
  mode: deep
  model: sonnet
  budget: 2.50
  max_turns: 30
  diff_limit: 2000
  on_build_fail: report
focus:
  priorities: ["auto"]
